<mah:MetroWindow x:Class="SampleApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:fluent="urn:fluent-ribbon"
        mc:Ignorable="d"
        Height="450" Width="800"
                 Loaded="MetroWindow_Loaded"
                 Closed="MetroWindow_Closed">
    <mah:MetroWindow.TitleTemplate>
        <DataTemplate>
            <fluent:RibbonTitleBar x:Name="RibbonTitleBar" 
                                   Header="{Binding Path=Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                                   DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType=Window}}"
                                   UseLayoutRounding="True" 
                                   SnapsToDevicePixels="True"
                                   HideContextTabs="False"
                                   />
        </DataTemplate>
    </mah:MetroWindow.TitleTemplate>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <fluent:Ribbon Grid.Row="0" x:Name="MainRibbon">
            <fluent:Ribbon.ContextualGroups>
                <fluent:RibbonContextualTabGroup x:Name="toolsGroup"
                                     Header="Tools" 
                                     Visibility="Visible"
                                     Background="Green" 
                                     BorderBrush="Green" />
            </fluent:Ribbon.ContextualGroups>
            <fluent:RibbonTabItem Header="Home">
                <fluent:RibbonGroupBox Header="Clipboard">
                    <fluent:Button Header="Copy" />
                    <fluent:Button Header="Cut" />
                    <fluent:Button Header="Paste" />
                </fluent:RibbonGroupBox>
            </fluent:RibbonTabItem>
            <fluent:RibbonTabItem Header="With Group" Group="{Binding Source={x:Reference toolsGroup}}">
                <fluent:RibbonGroupBox Header="Tools">
                    <fluent:Button Header="Tool 1" />
                    <fluent:Button Header="Tool 2" />
                    <fluent:Button Header="Tool 3" />
                </fluent:RibbonGroupBox>
            </fluent:RibbonTabItem>
        </fluent:Ribbon>
    </Grid>
</mah:MetroWindow>
